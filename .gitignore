# Rust build artifacts
/target
**/*.rs.bk
Cargo.lock.bak

# Secrets / credentials — NEVER commit
.env
.env.*
!.env.example
*.token
kite_token.json
.kite_token.json
nse_instruments_cache.json

# Huge local data archive (~14GB) — never commit
/1500-Stocks-Parquest/

# Generated caches (edge map, instruments, pre-market state)
/cache/
/data_out/

# macOS
.DS_Store
**/.DS_Store

# Runtime journal DB + CSV exports (not source)
/data/
/serve.log
# Session/run logs (backtest, download, shards)
*.log
